{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:client": "esbuild src/client/index.ts --bundle --outfile=public/client.js --platform=browser",
    "start": "ts-node-dev --respawn --transpile-only src/server/index.ts",
    "build:watch": "npm run build:client -- --watch",
    "dev": "concurrently \"npm run build:watch\" \"npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.2",
    "@types/socket.io": "^3.0.1",
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
